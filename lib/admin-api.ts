// Legacy wrapper kept for backwards compatibility.
// Redirects to the new typed services in lib/services.
export { newsletterService as newsletterAPI } from './services/newsletter'
export { blogService as blogAPI } from './services/blog'
